class { 'ufw': }

ufw::allow { 'ssh':
  port => '22'
}

ufw::allow { 'http':
  port => '443'
}

ufw::allow { 'https':
  port => '80'
}

ufw::allow { 'icinga':
  port => '5665'
}

ufw::allow { 'ssh/tcp':
  port => '22/tcp'
}

ufw::allow { 'http/tcp':
  port => '443'
}

ufw::allow { 'https/tcp':
  port => '80/tcp'
}

ufw::allow { 'http/https/tcp':
  port => '80,443/tcp'
}
