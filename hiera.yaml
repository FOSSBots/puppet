version: 5

defaults:
   datadir: hieradata
   data_hash: yaml_data

hierarchy:
  - name: "per-host Yaml data"
    path: "hosts/%{::hostname}.yaml"
  - name: "common secure data"
    path: "private/common.yaml"
  - name: "per-host secure data"
    path: "private/%{::hostname}.yaml"
